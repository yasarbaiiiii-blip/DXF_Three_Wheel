# Backend

This folder is reserved for the websocket backend that the tablet app connects to during development.

Expected usage:
- run the backend from a terminal on the laptop
- expose a websocket endpoint on the LAN, not only `localhost`
- point the connection screen at the laptop IP or discoverable websocket URL

Place the backend source here when you bring it into this workspace.
